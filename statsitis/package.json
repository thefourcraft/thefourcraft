{
  "name": "statsitis",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted GitHub profile stat-card renderers (streak, stats, graph). Pure TS library consumed by the dfwebsite Cloudflare Worker.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --experimental-strip-types --test src/stats.test.ts"
  }
}
